const express = require('express');
const router = express.Router();

router.get("/api/articulos", 
    async function(req, res, next) {
    //Código del método
    });

router.get("/api/articulos/:id", 
    async function (req, res, next) {
    //Código del método
    });

module.exports = router;